{
  "name": "todolist",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "db:create": "createdb todo_list && psql -d todo_list < src/models/db/schema.sql",
    "db:reset": "dropdb todo_list && createdb todo_list && psql -d todo_list < src/models/db/schema.sql",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.2",
    "ejs": "^2.5.7",
    "express": "^4.15.4",
    "morgan": "^1.8.2",
    "pg-promise": "^6.5.1"
  }
}
